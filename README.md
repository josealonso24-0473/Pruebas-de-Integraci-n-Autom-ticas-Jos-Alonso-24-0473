# Pruebas-de-Integraci-n-Autom-ticas-Jos-Alonso-24-0473

# 1. Instalar dependencias (solo la primera vez)
npm install

# 2. Ejecutar todo — levanta el mock server y corre las pruebas
node run-tests.js
```

El reporte HTML queda en `reports/postman_report.html`.
